# Исходники везде с LF — и в репозитории, и в рабочей копии.
# Явный eol=lf важен на Windows: иначе Git сам подставит CRLF при checkout
# и весь файл будет выглядеть изменённым.
* text=auto eol=lf

# Бинарные: не диффить и не трогать переносы строк
*.png binary
*.svg text
*.pen binary
